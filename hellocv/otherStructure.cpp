//输出其他常用数据结构
#include "pch.h"
//#include <opencv2/opencv.hpp>
//#include <opencv2/highgui/highgui.hpp>
//
//int main()
//{
	//using std::cout;
	//using std::endl;
	////定义和输出二维点
	//cv::Point2f p(6, 2);
	//cout << "【二维点】p = " << p << endl;
	////定义和输出三维点
	//cv::Point3f p3(3, 4, 5);
	//cout << "【三维点】p3 = " << p3 << endl;
	////定义和输出基于Mat的std::vector
	//std::vector<float> mv;
	//mv.push_back(3);
	//mv.push_back(4);
	//mv.push_back(5);
	//cout << "【基于Mat的vector】mv = " << cv::Mat(mv) << endl;
	//std::vector<cv::Point2f> points(20);
	//for (int i = 0; i < points.size(); i++)
	//	points[i] = cv::Point2f((float)i, (float)(i + 1));
	//cout << "【二维向量点】points = " << points << endl;

	//cv::Mat srcImg = cv::imread("./image/1.jpg");
	//if (!srcImg.data)	std::cout << "加载图片失败" << std::endl;
	//cv::imshow("原图", srcImg);
	//cv::Mat dstImg;
	////cv::cvtColor(srcImg, dstImg, cv::COLOR_BGR2Lab);
 //	cv::imshow("效果图", dstImg);
	//cv::waitKey(0);
//}